# TensorBoard 曲线说明

- `actor_loss_and_expert_constraint.png`: PPO actor 的 `drone/policy_loss`，以及专家动作约束 `drone/expert_kl_loss`、`alpha * expert_kl_loss`、`drone/expert_action_mse`。这次 run 没有单独的 `bc_aux_*` 曲线，因为 `cfg/algo/mappo.yaml` 中 `actor.bc_aux.enabled=false`，且 event 文件里没有 `bc_aux` tag。
- `critic_loss.png`: critic 的 `drone/value_loss` 和 `drone/critic_grad_norm`。
- `reward_return_and_success.png`: 训练/评估的 `stats.return` 与 `stats.success`。
- `requested_curves_overview.png`: 汇总图。
